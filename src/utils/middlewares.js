// Authorization with jsonwebtoken
//  - check session/cookie/token ? > not found > error
//  - querydb for resource > use_id logged matches with the same of the query response of the database? no > unauthorized error > ok: respond with the resource.
