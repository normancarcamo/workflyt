-- CAT = category:
drop sequence if exists nz.category_seq cascade;

-- CMP = company:
drop sequence if exists nz.company_seq cascade;

-- CUS = customer:
drop sequence if exists nz.customer_seq cascade;

-- DEP = department:
drop sequence if exists nz.department_seq cascade;

-- IOR = installation_order:
drop sequence if exists nz.installation_order_seq cascade;

-- WOR = work_order:
drop sequence if exists nz.work_order_seq cascade;

-- PRM = permission:
drop sequence if exists nz.permission_seq cascade;

-- SRV = service:
drop sequence if exists nz.service_seq cascade;

-- PRD = product:
drop sequence if exists nz.product_seq cascade;

-- MTR = material:
drop sequence if exists nz.material_seq cascade;

-- QTE = quote:
drop sequence if exists nz.quote_seq cascade;

-- IVC = invoice:
drop sequence if exists nz.invoice_seq cascade;

-- ROL = role:
drop sequence if exists nz.role_seq cascade;

-- SPL = supplier:
drop sequence if exists nz.supplier_seq cascade;

-- USR = user:
drop sequence if exists nz.user_seq cascade;

-- WRH = warehouse:
drop sequence if exists nz.warehouse_seq cascade;

-- EMP = employee:
drop sequence if exists nz.employee_seq cascade;
