-- you have to have a user called "test" before to continue:
-- set session authorization test;

-- the following statement will show you the current user + current session user:
-- select current_user, session_user;

-- if you don't want to follow using the session as "test", then reset it:
-- reset session authorization;
